
// one overarching function
// return(checking,savings)

function of bank balance one
var checking = function(oldBalance,newBalance) {
  var newBalance = function(destination) {
    if (destination === newBalance) {
      return (newBalance + amount);
    }else  {
      return (newBalance - amount);
    };
    //put this back in after else if if necessary
// if (destination === newBalanceSavings)
//function for new balance after transfer

//function of transfer
var transfer = (amount, destination)
var destination =  function() {
  if (newBalance) {
    return (oldBalance + amount)
  }else if(newBalanceSavings){
// input opposite into other function
    return ((oldBalanceSavings + amount)
  };
}
//function for new balance after transfer

//function of bank balance two

var savings = function(oldBalanceSavings,newBalanceSavings){
  var newBalanceSavings function(destination) {
    if (destination === newBalanceSavings) {
      return (newBalanceSavings + amount);
    }else if (destination === newBalance) {
      return (newBalance - amount);
    }
  }
//
//
//
// return(checking,savings)

//
// Need beginning amount to play with
