//write function to break up phase and run each through
//if
for (var counter = 0; counter < phrase.length; counter++) {
  if (true) {
  (messageArray[counter].match(/[d-z]/) || messageArray[counter].match(/[D-Z]/))
  }

var ceasarShift = function(phrase){
  var str = phrase;
  var makeSmall = str.toLowerCase();
  var shiftedChar =  String.fromCharCode(makeSmall.charCodeAt() - 3);
  decodedMessageArray.push(shiftedChar);
  }
